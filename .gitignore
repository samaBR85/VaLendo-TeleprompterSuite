node_modules/
out/
dist/
release/
*.log
# projetos salvos ao testar o app não são código do app
teste_projetos/

# o perfil do `npm run start:debug`: workspace, presets e preferências do app
# de TESTE. Fica dentro do repositório para ser fácil de achar e de apagar
# quando uma checagem deixa sujeira — apagar a pasta devolve um app novo em
# folha, sem tocar no perfil de trabalho de verdade
.perfil-de-teste/
.DS_Store
Thumbs.db

# o ffmpeg compilado na CI para o macOS; é artefato de build, não fonte
.ffmpeg-build/

# os PNG intermediários do ícone: saem dos SVG de build/icone/ rodando
# `node scripts/gerar-icones.mjs`. O que entra no repositório é a FONTE (os
# três SVG) e o que o empacotador consome (icon.ico, icon.png)
build/icone/png/
